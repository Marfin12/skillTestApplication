import Constants from '../../Constants';

const defaultProps = {
  children: null,
  style: {},
  onPress: Constants.UTILS.EMPTY_FUNCTION,
};

const displayName = 'Card';

export default {
  defaultProps,
  displayName,
};
