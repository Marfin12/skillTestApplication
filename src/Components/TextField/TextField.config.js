const defaultProps = {
  style: {},
  textStyle: {},
  darkThemeColor: '#FFF',
  defaultThemeColor: '#111',
  numberOfLines: 0,
};

const displayName = 'TextField';

export default {
  defaultProps,
  displayName,
};
